--- src/screen.c
+++ src/screen.c
@@ -1206,15 +1247,14 @@
 		continue;
 
 	    default:
+#if 0
 #ifdef MULTICHAR_SET
-		if( r->encoding_method == ENC_NOENC )
+		if (r->encoding_method == ENC_NOENC)
 		{
-		    /* 2009-03-29 gi1242 TODO: Deal with ISO-8859 encodings. */
 		    if (c == 127)
 			continue;
 		    break;
 		}
-
 		PVTS(r, page)->rstyle &= ~RS_multiMask;
 
 		/* multibyte 2nd byte */
@@ -2624,18 +2757,18 @@
 		    *(s--)  = '\0';
 		    *s	    = 'm';
 
-		    fprintf( fd, "%s%.*s\e[0m", escsq, col-start, txt+start );
+		    fprintf( fd, "%s%.*s\e[0m", escsq, col-start, (char*) txt+start );
 		}
 
 		else
-		    fprintf( fd, "%.*s", col - start, txt + start );
+		    fprintf( fd, "%.*s", col - start, (char*) txt + start );
 
 	    }
 	} /* if( pretty ) */
 
 	else
 	    /* Vanilla text */
-	    fprintf( fd, "%.*s", (lineEnd + 1), txt );
+	    fprintf( fd, "%.*s", (lineEnd + 1), (char*) txt );
 
 	if( !linecont || PSCR(r, page).tlen[row+row_offset] != -1 )
 	    fputc( '\n', fd );
